# Provenance records

Historical restoration/import records live here when they need more detail than Git commit metadata alone.

These directories are **documentation only**. They must not contain duplicate active source trees or become alternate scientific pipelines.

Historical version/date labels are appropriate here when they identify a specific imported/restored state.

Current record:

- `methodology_restoration_2026-09-07/` — validation and archive identity for the historical methodology state previously labelled `methodology_v5`.

For active code/report paths, use purpose-based names elsewhere in the repository. Retrieve historical implementation from Git history rather than recreating source snapshots under this directory.
