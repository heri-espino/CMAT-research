# CMAT methodology report

This directory is an **atomic methodology/statistical-review workspace**. It contains the report source, retained aggregate tables/figures, brainstorming notes, provenance and its own thin execution entry point.

It does **not** own a separate scientific implementation: reusable functions remain in the repository-level `code/` package.

Before substantive analytical work here, read `../AI_HANDOFF.md` and `../../docs/RESEARCH_WORKFLOW.md`. Those documents define the general rule that this report is an experimental/research workspace using root `code/` as its scientific library.

## Atomic structure

```text
brainstorm/methodology_report/
├── README.md
├── code/
│   └── methodology_report.py      # thin local entry point
├── tables/                        # retained aggregate CSV + LaTeX snippets
├── figures/                       # retained aggregate figures
├── notes/                         # report-level brainstorming/future analyses
├── provenance/                    # report-specific historical/method records
├── build/                         # disposable local generated outputs; ignored by Git
├── methodology_report.tex
├── methodology_report.pdf
└── referencias.bib
```

## Root-code dependency rule

`code/methodology_report.py` should only locate/import the canonical root-code package and invoke the report recipe. Do not add reusable statistics, cohort definitions, transformations, models or plotting functions here.

The implementation used by this report lives in root `code/`, principally:

- `../../cmat_analysis/src/cmat_analysis/study/methodology_pipeline.py` — analysis sequence;
- `../../cmat_analysis/src/cmat_analysis/study/extended_methodology.py` — methodology extensions;
- `../../cmat_analysis/src/cmat_analysis/study/methodology_plots.py` — figures;
- `../../cmat_analysis/src/cmat_analysis/reporting/methodology_report.py` — LaTeX table rendering;
- `../../cmat_analysis/src/cmat_analysis/reporting/methodology_build.py` — build/validation orchestration.

If the report needs a new scientific function, add it to root `code/`, test it there, regenerate `cmat_analysis/FUNCTION_INDEX.md`, and only then call it from the local runner.

## How to add a new methodology experiment

When a new check or methodological idea is needed:

1. record the question/rationale in this README or `notes/`;
2. search `../../cmat_analysis/FUNCTION_INDEX.md`;
3. reuse an existing canonical function when possible;
4. if a reusable calculation is missing, implement it under `../../cmat_analysis/src/cmat_analysis/` with tests/documentation;
5. call it from `code/methodology_report.py` only through the canonical root-code API/build recipe;
6. generate diagnostics/tables/figures under the report workflow;
7. preserve assumptions, null findings, sensitivities and interpretation here even when they will not enter a paper;
8. promote only reviewed evidence downstream into the relevant paper.

Do not calculate a new paper number directly in a manuscript and then retrofit this report later.

## Reproducible build

From the repository root:

```bash
# Validate paths/import structure without controlled data
python brainstorm/methodology_report/code/methodology_report.py --check

# Run with explicit controlled data extracts
python brainstorm/methodology_report/code/methodology_report.py \
  --materias /controlled/path/Materias.xlsx \
  --asesorias /controlled/path/Asesorias.xlsx

# Regenerate, stage, and compile the report
python brainstorm/methodology_report/code/methodology_report.py \
  --materias /controlled/path/Materias.xlsx \
  --asesorias /controlled/path/Asesorias.xlsx \
  --compile
```

The stable runner imports the methodology pipeline/functions from root `code/`, executes them in the fixed report recipe, stages generated CSV/PNG assets into this directory, and rebuilds `latex_*.tex` table snippets.

Disposable intermediate outputs and the build manifest live under:

`build/`

This directory is ignored by Git. Reviewed/staged report assets live in `tables/` and `figures/`.

To generate working outputs without replacing the retained report tables/figures:

```bash
python brainstorm/methodology_report/code/methodology_report.py \
  --materias <academic-file> \
  --asesorias <visits-file> \
  --no-stage
```

## Role in the broader repository

This report sits between root code and publication papers:

```text
root/code
    ↓
methodology_report
    broad methodology + validated analyses + sensitivities
    ↓
papers/
    manuscript-specific selection of relevant evidence
```

The report should remain broader than any single paper. A result may be retained here even when Paper 1 or Paper 2 does not use it.

## Important boundary: refined N=3,241 snapshot

The refined PPA longitudinal N=3,241 result was produced in a later development stage and is **not** generated by the methodology pipeline. The runner deliberately preserves and labels these assets as external later-stage snapshots rather than claiming they were produced by the methodology recipe:

- `tables/90_refined_longitudinal_persistence_summary_snapshot.csv`
- `tables/latex_longitudinal_refined_3241.tex`
- `figures/refined_longitudinal_persistence_3241.png`

This boundary must remain explicit until the refined longitudinal stage is reconciled into the canonical root-code pipeline.

## Narrative prose after a data update

Tables, figures and LaTeX table snippets are regenerated computationally. `methodology_report.tex` still contains explanatory prose with embedded numerical values; after a new data extract changes results, review those sentences against regenerated outputs before treating the compiled PDF as updated.

Do not manually change a numerical claim without tracing it to the corresponding generated output.

## Main retained files

- `methodology_report.pdf` — compiled report artifact.
- `methodology_report.tex` — LaTeX source.
- `referencias.bib` — report bibliography.
- `figures/` — aggregate figures used by the report.
- `tables/` — aggregate CSV outputs plus generated LaTeX table snippets.
- `notes/` — report-level planning/brainstorming material.
- `provenance/` — historical/statistical/privacy records for this report.

## Methodological content

The report documents, among other items:

1. official degree programme from academic records linked by student ID;
2. BV/RT/BA continuous imputation using within-classroom KDE below 7.5 with SciPy/Scott default and explicit fallbacks;
3. exact performance groups `0/1/2/3/4+`, Games–Howell pairs and adjusted FE+Holm pairs;
4. exact counts 0–12 with sparse-tail diagnosis;
5. temporal/periodicity analyses for CMAT and first-MU populations;
6. degree-programme summaries and use-versus-performance analyses;
7. the N=4,211 future-Calculus progressor sensitivity;
8. limitations involving ecological inference, cluster-robust inference, multiplicity, selection and propensity-score approaches.

## Scientific provenance

Historical source fingerprint associated with the restored methodology snapshot:

`03dd3d4ddd31cc2263be54900e1b25749fab8a7beb2033fbe796bf240fd4c39f`

Historical tests at freeze: **9 passed**. The active root-code repository now contains dedicated methodology helper tests plus the shared current test suite.

Version labels such as `methodology_v5` are retained only where they identify historical provenance files/fingerprints. The active report path remains purpose-based and versionless.

## Privacy

The report excludes the original administrative workbooks and should contain only aggregate research products/documentation. It must not be described as proof of absolute anonymity. See `provenance/PRIVACY_AND_RELEASE.md`.