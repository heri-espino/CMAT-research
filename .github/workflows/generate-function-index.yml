name: Generate cmat_analysis function index

on:
  push:
    branches: ['main']
    paths:
      - 'cmat_analysis/**/*.py'
      - '.github/workflows/generate-function-index.yml'

permissions:
  contents: write

concurrency:
  group: generate-cmat-analysis-function-index
  cancel-in-progress: true

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.14'
      - name: Generate AST index
        run: python cmat_analysis/scripts/generate_function_index.py
      - name: Commit generated index
        run: |
          if test -z "$(git status --porcelain -- cmat_analysis/FUNCTION_INDEX.md)"; then
            echo 'Index is already current.'
            exit 0
          fi
          git config user.name 'CMAT documentation bot'
          git config user.email 'actions@users.noreply.github.com'
          git add cmat_analysis/FUNCTION_INDEX.md
          git commit -m 'docs: generate cmat_analysis function index'
          # The repository contains historical Git-LFS files that checkout may
          # leave as harmless working-tree modifications. A pull --rebase would
          # reject that dirty tree even though the generated index is committed.
          # This job starts from the triggering main commit, so the immediate
          # fast-forward push is the appropriate operation here.
          git push origin HEAD:main
