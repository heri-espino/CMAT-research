# Paper

Publication manuscript and build assets belong here. `PROJECT_CONTEXT.md` preserves the pre-branch paper planning record when available. Numerical claims must remain traceable to `results/`, `brainstorm/`, and the validated `cmat_analysis` implementation.
